/**
 * Vitest setup — runs before each test file.
 *
 * Imports jest-dom matchers (toBeInTheDocument etc.) and sets up
 * any global polyfills needed for component tests.
 */

import "@testing-library/jest-dom/vitest";
